name: "hyperliquid"

services:
  node:
    platform: linux/amd64
    restart: unless-stopped
    build: .
    ports:
      - "4000-4010:4000-4010"
    volumes:
      - /data/hyperliquid/hl-data:/home/hluser/hl/data
      - ./override_gossip_config.json:/home/hluser/override_gossip_config.json

  pruner:
    restart: unless-stopped
    build: ./pruner
    volumes:
      - /data/hyperliquid/hl-data:/home/hluser/hl/data
